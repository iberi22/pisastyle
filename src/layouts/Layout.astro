---
import { Toaster } from '@swal/ui';
interface Props { title?: string; }
const { title = 'SWAL App — Template' } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content="SWAL App Template — Astro + Svelte + @swal/ui + vite-pwa + Cloudflare + Xavier + mesh + LLM" />
    <link rel="manifest" href="/manifest.json" /><meta name="theme-color" content="#06b6d4" />
  </head>
  <body>
    <slot />
    <Toaster client:load />
    <style is:global>
      @import '@swal/ui/tokens';
      html { background: var(--swal-bg); color: var(--swal-text); }
      body { margin: 0; font-family: var(--swal-font); -webkit-font-smoothing: antialiased; }
    </style>
  </body>
</html>
